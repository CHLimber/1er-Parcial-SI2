===============================================================================
 FASHIONSTORE - PENDIENTES
 Relevado el 2026-09-13 sobre el estado real del repo y de los dos despliegues
 (Docker local y Railway). Cada punto dice que falta, donde, y como se resuelve.
===============================================================================

-------------------------------------------------------------------------------
 1. BLOQUEANTE EN LA NUBE (Railway)
-------------------------------------------------------------------------------

[1.1] La matriz de permisos de CU13 esta vacia en la base de Railway.
      ESTADO   : diagnosticado, arreglo escrito y validado, FALTA APLICARLO.
      SINTOMA  : el login funciona y el catalogo carga, pero todo CU09-CU13
                 responde 403 "Tu rol no tiene permiso para esta operacion",
                 incluso con admin@fashionstore.bo.
      EVIDENCIA: POST /auth/login devuelve rol "ADMIN" con permisos: []
                 403 en /admin/usuarios, /admin/roles, /admin/permisos,
                 /recepciones, /proveedores, /admin/sucursales.
      CAUSA    : las tablas permiso / rol_permiso quedaron sin poblar
                 (03_datos_iniciales.sql aplicado a medias).
      AFECTA   : la web y la app movil por igual; la API autoriza por codigo de
                 permiso, asi que ningun rol puede hacer gestion.
      ARREGLO  : db/reparaciones/permisos_cu13.sql (idempotente, no toca
                 usuarios/catalogo/inventario). Ya probado contra la base local:
                 corre sin insertar nada y deja los conteos intactos.

                   cd fashionstore
                   docker run --rm -i postgres:16 psql "<DATABASE_PUBLIC_URL>" \
                     -v ON_ERROR_STOP=1 < db/reparaciones/permisos_cu13.sql

                 Debe imprimir: ADMIN 19, ENCARGADO 11, ALMACEN 7, CAJERO 4,
                 VENDEDOR 3. Despues hay que volver a iniciar sesion (o tocar
                 "actualizar permisos" en la app): el JWT no lleva los permisos,
                 pero la sesion guardada si, y queda vieja hasta refrescarla.
      FALTA     : el DATABASE_PUBLIC_URL del servicio Postgres de Railway
                 (pestana Variables; la publica, no DATABASE_URL).

[1.2] Verificar el resto del seed en Railway despues de 1.1.
      No se pudo comprobar cuantos usuarios/sucursales/recepciones tiene la base
      de la nube porque esos endpoints devuelven 403 por el punto anterior. Lo
      unico confirmado es que existen admin@fashionstore.bo, cliente@fashionstore.bo
      y 6 productos de catalogo. Repetir el conteo cuando 1.1 este aplicado.

-------------------------------------------------------------------------------
 2. ALCANCE DE LOS CU QUE QUEDO INCOMPLETO
-------------------------------------------------------------------------------

[2.1] CU10 "Gestionar Catalogo (incluye promociones)": las promociones NO se
      gestionan en ningun lado.
      El backend solo las consume: /ventas/checkout valida el codigo de cupon
      contra la tabla promocion y el webhook devuelve el uso si el pago falla.
      No hay alta/baja/edicion de promociones ni en la API, ni en la web, ni en
      el movil. Falta: admin_router de promociones + pantalla en panel-catalogo
      (web) + pestana en panel_catalogo_pagina.dart (movil).

[2.2] CU07: falta el cierre de caja.
      /caja/abrir crea la sesion, pero no existe ningun endpoint para cerrarla
      ni para el arqueo. Una sesion ABIERTA queda abierta para siempre, lo que
      ademas bloquea esa caja para otro cajero.

[2.3] CU04 E2: nadie llama a fn_expirar_reservas().
      La funcion existe en db/02_logica.sql y libera el stock comprometido por
      reservas vencidas, pero el backend no tiene el job periodico (APScheduler
      o cron) que la invoque. Hoy la unica forma de liberar una reserva vencida
      es que un Encargado la marque "no se presento" a mano desde CU08.

[2.4] [HECHO 2026-09-18] Entrega a DOMICILIO: cerrado por CU20 (ver 2.12).
      Era: CheckoutIn aceptaba entrega=DOMICILIO + direccion_id pero no habia
      endpoints para crear/listar direcciones, asi que web y movil solo podian
      ofrecer RETIRO_SUCURSAL. Ahora existe el CRUD /direcciones (con mapa en
      las dos UIs), el checkout cotiza y cobra la tarifa, y el envio se sigue
      hasta la puerta.

[2.5] [HECHO 2026-09-18] CU15 "Consultar Reportes e Indicadores": backend + web.
      Modulo nuevo app/modules/reportes/ (permiso unico reportes.leer, ya sembrado
      para ADMIN y ENCARGADO): 6 endpoints, sin capa de servicio, SQL directo en el
      router (no se toco db/02_logica.sql; v_ventas_diarias no se reusa porque no
      expone sucursal_id para filtrar, se reescribio la misma agregacion en el
      router). "Dinamicos" (aceptan desde/hasta/sucursal_id, ventas-diarias ademas
      canal): /reportes/indicadores (KPIs), /reportes/ventas-diarias,
      /reportes/ventas-por-sucursal, /reportes/top-productos. "Estaticos" (foto
      actual, sin fecha): /reportes/stock-por-sucursal, /reportes/reservas-por-estado.
      Mismo patron de scoping por sucursal que recepciones.py: un ENCARGADO solo ve
      su sucursal (403 si pide otra), un ADMIN (el que tiene sucursales.actualizar)
      ve la cadena completa y puede filtrar. Probado con curl contra Postgres real:
      los 6 endpoints, el 403 cruzando sucursal como ENCARGADO y el 403 de un
      CLIENTE sin el permiso.
      OJO tipos: los campos de plata quedaron en `float` (no Decimal) en
      app/modules/reportes/schemas.py -- Pydantic v2 serializa Decimal como string
      en el JSON y el frontend (y Chart.js) esperan number. catalogo/schemas.py ya
      usaba float por lo mismo; recepciones/schemas.py sigue en Decimal y por eso
      sus montos le llegan al frontend como string (no verificado si algo lo nota).
      Web: pages/panel-reportes/ (ruta /panel/reportes, agregada a panel.page.ts y
      panel-shell.ts) con Chart.js (dependencia nueva, npm install chart.js) --
      paleta de marca (--flame/--gold) para series unicas, paleta categorica
      validada con la skill dataviz (azul/naranja/aqua, no es la paleta de marca)
      solo para el grafico con series de verdad (ventas diarias por canal). Filtro
      de sucursal en la UI solo se muestra si el usuario tiene sucursales.actualizar
      (mismo criterio que el backend). Verificado: tsc --noEmit limpio, `ng build`
      limpio, `ng serve` compila.
      FALTA (a proposito, ver charla con el usuario):
        - Movil: panel_reportes_pagina.dart no se hizo, CU15 queda solo en backend+web.
        - "Consultas por voz con IA": en la pantalla web hay una seccion
          "Consultas por voz (proximamente)" que es placeholder puro -- sin
          endpoint, sin ANTHROPIC_API_KEY en .env, sin dependencia del SDK de
          Claude. Es una tarea aparte, CU15 (reportes) ya esta cerrado sin ella.

[2.6] Ajustes manuales de stock: idem.
      El permiso inventario.ajustar esta asignado a ADMIN, ENCARGADO y ALMACEN,
      y fn_mover_inventario() acepta ajustes, pero no hay endpoint que los
      registre. Segun CLAUDE.md, un ajuste es la unica forma de corregir una
      recepcion ya confirmada (el kardex es append-only), asi que hoy esa
      correccion no se puede hacer desde la aplicacion.

[2.7] Tablas del esquema sin ningun uso en el backend (0 referencias):
      traspaso, devolucion. Estan en db/01_schema.sql pero ningun modulo las toca.
      Decidir si entran en el alcance o se documentan como fuera de el.
      [CORREGIDO 2026-09-18] Esta entrada decia tambien "resena, favorito" -- no
      existen en db/01_schema.sql (se reviso completo al implementar CU17, que
      necesitaba saber que senales de historial habia disponibles). No hay tabla
      de resenas ni de favoritos en el esquema actual; el dato mas parecido es
      usuario.color_favorito_id (una preferencia de color, no una tabla de
      favoritos de productos).
      NOTA (modelo de negocio, ver README): traspaso es el que mas justificacion
      tiene de los cuatro. Las 3 sucursales estan en 3 climas distintos y el
      seed ya reparte el stock de forma asimetrica (el tapado se acumula en
      Santa Cruz y falta en La Paz), asi que el traspaso entre tiendas es la
      operacion natural del negocio, no una tabla decorativa. Si se implementa
      solo una de las cuatro, que sea esta.

[2.8] [HECHO 2026-09-18] CU16 "Usar Vestidor Virtual (RA)": movil, los dos
      niveles de IMAGENES_AR.txt. Web queda afuera (CU16 se pidio solo para
      movil).

      NIVEL 2 (AR_MODELO, GLB, <<mejor cambio por hora de trabajo>>):
        - 2 modelos gratuitos bajados de poly.pizza sin login, en
          backend/app/media_semilla/ar/: bot-004-ar.glb (1.76 MB) y
          san-004-ar.glb (20.8 KB). Licencia CC BY 3.0 en ambos -- atribucion
          obligatoria: "Cowboy boots" de Poly by Google
          (https://poly.pizza/m/9Qf1MHGePvS) y "Sandal" de jeremy
          (https://poly.pizza/m/4_4lQugjiRa).
        - DOS PRODUCTOS NUEVOS EN VEZ DE FORZAR LOS QUE HABIA. Al renderizar
          los modelos para revisarlos se vio que no son lo que decian los
          productos a los que se los habia colgado: el primero es una bota
          texana con espuelas (no una botineta de tobillo) y el segundo una
          sandalia deportiva de tiras planas (no una de plataforma). Colgarlos
          ahi significaba que la clienta veria en RA algo distinto de lo que
          compra, asi que se dieron de alta como productos propios en
          db/03_datos_iniciales.sql: BOT-004 "Bota Texana de Cuero"
          (Otonio-Invierno, Bs 549.90) y SAN-004 "Sandalia Deportiva de Tiras"
          (Primavera-Verano, Bs 229.90), los dos destacados, con variantes de
          talla 35-39, color nuevo "Turquesa" (#009688, el de las tiras del
          modelo) e inventario repartido por el mismo criterio de clima que el
          resto (la bota pesa en La Paz 290 vs 37 en Santa Cruz; la sandalia al
          reves, 233 en Santa Cruz vs 34 en La Paz). BOT-001 y SAN-001 volvieron
          a quedar sin AR.
        - LA FOTO DE CATALOGO DE ESOS DOS ES UN RENDER DEL PROPIO GLB
          (backend/app/media_semilla/catalogo/bota-texana-de-cuero.jpg y
          sandalia-deportiva-de-tiras.jpg), hecho con un rasterizador propio
          que samplea la textura del modelo. Asi la foto del catalogo y lo que
          se ve en RA son exactamente la misma prenda, sin buscar fotos de
          stock que volverian a no coincidir.
        - ESCALA CORREGIDA (era un bug que rompia la demo): los dos modelos
          venian de poly.pizza en unidades arbitrarias -- la bota media 2.58
          "metros" de alto y la sandalia 9.65 de largo -- y Scene Viewer los
          planta en RA con ese tamano, o sea una bota de dos pisos. Se
          reescalaron y se apoyaron en el piso (Y=0) envolviendo las raices de
          la escena en un nodo con matriz de escala, sin tocar la geometria:
          ahora la bota mide 31x36x35 cm y la sandalia 13x8x26 cm, como pide
          IMAGENES_AR.txt punto 3.
        - MIME TYPE (otro bug que rompia la demo): StaticFiles servia los .glb
          como `text/plain` porque Python no conoce el formato, y Scene Viewer
          rechaza el modelo. app/main.py ahora registra `model/gltf-binary`
          (.glb) y `model/vnd.usdz+zip` (.usdz) con `mimetypes.add_type` antes
          de montar /media. Verificado con curl: devuelve model/gltf-binary.
        - SIN USDZ: convertir GLB a USDZ pide Reality Converter (Mac), y se
          desarrollo en Windows. movil/lib/paginas/producto_detalle_pagina.dart
          (`_vestidorVirtual`) por eso solo muestra el boton "VER EN 3D" en
          Android (`Platform.isAndroid`); en iOS se ve un aviso en vez del
          boton. Pendiente real si se consigue una Mac o un conversor online.
        - El boton llama a `_abrirVisor3d`, que arma un intent:// de Google
          Scene Viewer y lo abre con url_launcher
          (`LaunchMode.externalApplication`). Se usa `mode=ar_preferred` y NO
          `ar_only`: con ar_only, un equipo sin ARCore no abre absolutamente
          nada; con ar_preferred siempre se ve el modelo en 3D y el boton de
          "ver en tu espacio" aparece solo si el equipo lo soporta.
          AndroidManifest.xml declara `<package android:name="com.google.ar.core"/>`
          en `<queries>` (si no, `canLaunchUrl` da false en Android 11+).

      NIVEL 1 (AR_OVERLAY, PNG con transparencia, camara en vivo):
        - 3 overlays en backend/app/media_semilla/ar/: blu-001-ar-blanco.png,
          ves-003-ar-rosa.png, abr-001-ar-camel.png. Recortados con un script
          propio (Pillow + scipy.ndimage.label: separa el fondo por color Y
          por estar conectado al borde de la imagen, asi no se come zonas
          internas de la prenda del mismo color que el fondo -- ver el
          historial de esta sesion si hace falta regenerarlos, el script no
          quedo en el repo por ser una herramienta de una sola vez).
        - anclajes (JSONB) y escala_base (NUMERIC) medidos a ojo sobre cada
          PNG con una grilla de referencia, como sugiere IMAGENES_AR.txt
          punto 4.1. Cargados en el seed (db/03_datos_iniciales.sql, bloque
          "CU16 Usar Vestidor Virtual (RA)").
        - [5.1 CERRADO] admin_router.py `_crear_fila_imagen` ahora si guarda
          anclajes/escala_base (antes quedaban siempre NULL); admin_schemas.py
          (`ImagenIn`/`ImagenOut`) y catalogo/schemas.py (`ImagenArOut`) los
          exponen. Probado end-to-end con curl contra Postgres real: POST
          .../imagenes con anclajes+escala_base, GET detalle admin y GET
          detalle publico devuelven ambos campos completos; fila de prueba
          borrada despues. `subir_imagen` (multipart) NO recibio los mismos
          campos -- solo hace falta si el panel de CU10 llega a subir un PNG
          de overlay a mano; por ahora todo entra por el seed SQL.
        - GET /catalogo/productos/{slug} suma el campo `ar` (lista de
          `ImagenArOut`: uso, formato, url, color, anclajes, escala_base) via
          un JOIN nuevo sobre producto_imagen con uso IN (AR_OVERLAY,
          AR_MODELO). Es la segunda lectura de una columna jsonb del backend
          (la primera fue auditoria/CU19) -- mismo patron: asyncpg no tiene
          codec jsonb (app/core/db.py), se lee como texto y se parsea con
          json.loads a mano (`_parsear_anclajes`, repetido en router.py y
          admin_router.py a proposito, cada modulo mantiene lo suyo).
        - movil/lib/paginas/vestidor_virtual_pagina.dart: camera + ML Kit
          Pose Detection (`google_mlkit_pose_detection`, agregado a
          pubspec.yaml junto con `camera`). Arma una transformacion de
          similitud (rotar+escalar+trasladar) que lleva hombro_izq/hombro_der
          del PNG exactamente sobre los hombros detectados -- por eso NO usa
          escala_base para nada (alcanza con los dos anclajes + la
          correspondencia de puntos; escala_base queda igual disponible por
          si mas adelante se quiere comparar contra el ancho de hombros real
          de la clienta, p.ej. para sugerir talla). cintura tampoco se usa
          todavia (reservado para un ajuste no-uniforme futuro).
        - PERMISOS: CAMERA agregado a AndroidManifest.xml (+ uses-feature
          camera/autofocus no requeridos) y NSCameraUsageDescription a
          ios/Runner/Info.plist.
        - FALTA PROBARLO CON UNA PERSONA DELANTE (no se pudo en la sesion que
          lo escribio). Si la prenda se ve cruzada de lado, NO hace falta
          tocar codigo: la barra del visor tiene un boton "invertir lados"
          (icono de flip) que intercambia que hombro detectado va a que
          anclaje del PNG. Se agrego justamente porque si la camara frontal
          entrega el buffer ya espejado depende del equipo.

      COMO PROBARLO (material disponible hoy):
        - MODELOS 3D, sin telefono: abrir en cualquier navegador
          http://localhost:8081/media/ar/previsualizar.html -- una pagina de
          verificacion (no es parte del producto) que muestra los dos GLB con
          <model-viewer>. Sirve para confirmar de un vistazo que cargan, que
          se ven bien y que estan a escala.
        - MODELOS 3D en el emulador: el AVD Pixel_10a es imagen con Play Store
          (google_apis_playstore, x86_64), asi que se puede instalar "Google
          Play Services para RA" desde la Play Store del propio emulador y
          Scene Viewer funciona contra la escena virtual de la camara trasera.
          Sin esa app instalada el intent no resuelve y cae al
          browser_fallback_url (descarga el .glb), que es justamente lo que
          `mode=ar_preferred` evita en un equipo real.
        - OVERLAY 2D en el emulador: la camara frontal del AVD estaba en
          `emulated` (una escena sintetica donde no hay ninguna persona, asi
          que la deteccion de pose nunca encuentra hombros). Se cambio a
          `webcam0` en ~/.android/avd/Pixel_10a.avd/config.ini para que use la
          webcam de la maquina y se pueda probar parandose delante de la
          laptop. Queda el respaldo config.ini.bak-antes-cu16 para revertirlo.
        - Prendas con overlay: blusa de lino, vestido de punto, tapado de lana.
          Prendas con modelo 3D: Bota Texana de Cuero, Sandalia Deportiva de
          Tiras (las dos destacadas, aparecen primeras en la tienda).

      DESPLEGADO EN RAILWAY [2026-09-18]:
        - Se pusheo todo el trabajo pendiente (CU15 a CU19) y Railway
          redesplego el backend: /media/ar/*.glb y *.png se sirven publicos por
          HTTPS y con Content-Type model/gltf-binary. Eso es lo que hace
          posible el nivel 2 en un telefono real: Scene Viewer es una app
          aparte de Google que descarga el .glb por su cuenta, no le sirve el
          backend local por HTTP.
        - La base de Railway tenia todavia el seed viejo de 6 productos (nunca
          se le habia aplicado el catalogo de 40), y 01_schema.sql no se puede
          re-aplicar encima porque los CREATE TYPE/TABLE no son IF NOT EXISTS.
          Se hizo respaldo con pg_dump, DROP SCHEMA public CASCADE y se
          reaplicaron 01+02+03. Ahora tiene los 42 productos.
        - Despues del seed hay que reescribir las URLs de imagen, porque el
          seed las hardcodea a http://localhost:8081 (la app movil las reescribe
          sola, pero la WEB no):
            UPDATE producto_imagen SET url = replace(url,'http://localhost:8081','<url-backend-railway>')
            WHERE url LIKE 'http://localhost:8081%';
          Se aplico: 199 filas.
        - OJO con scripts/aplicar_en_railway.sh: usaba la imagen postgres:16 y
          el plugin de Railway ya corre Postgres 18. psql 16 igual habla con un
          servidor 18, pero pg_dump 16 NO puede respaldarlo. Se cambio a
          postgres:18.
        - Si en el futuro NO se quiere recrear la base entera, existe
          db/reparaciones/cu16_vestidor_virtual.sql, que agrega solo lo de CU16
          (color, los 2 productos, variantes, inventario, fotos y filas AR) y es
          idempotente. Probado: aplica limpio sobre una base sin CU16 y la
          segunda corrida no inserta nada. Necesita que el catalogo de 40
          productos ya exista (usa las categorias botas/sandalias).

      VERIFICADO EN ESTA ENTREGA:
        - `docker compose down -v && up --build` (obligatorio por el cambio en
          03_datos_iniciales.sql) sin errores en el init de Postgres.
        - curl contra el backend real: los 3 productos con overlay devuelven
          sus anclajes y escala_base; los 2 nuevos devuelven su AR_MODELO;
          BOT-001/SAN-001 devuelven `ar: []`; un producto sin AR tambien.
          Los 5 archivos se sirven por /media/ar (200) y los .glb con
          Content-Type model/gltf-binary.
        - El catalogo pasa de 40 a 42 productos, los 2 nuevos con foto, stock
          en las 3 sucursales y variantes de talla/color.
        - CU10 end-to-end: POST .../imagenes con anclajes+escala_base los
          guarda y los devuelve (fila de prueba borrada despues).
        - movil: `flutter build apk --debug` OK (prueba que camera y
          google_mlkit_pose_detection compilan de verdad contra Android),
          `flutter analyze` sin issues, `flutter test` 3/3.

[2.9] [HECHO 2026-09-13] Catalogo de demostracion: ya son 40 productos en 9
      categorias (Faldas/Sandalias/Zapatos nuevas), 6 marcas, 9 destacados
      repartidos entre categorias y temporadas. Cargado en
      db/03_datos_iniciales.sql, probado con `docker compose down -v && up
      --build` contra Postgres real: 40 productos, 40 producto_imagen, 1290
      filas de inventario (40 variantes-set x 3 sucursales), sin huecos.
      Detalle completo (por que este reparto, checklist de verificacion) en
      CATALOGO_DEMO.txt.
      [2026-09-13, mas tarde] Las 40 fotos reales (packshot 3:4) se subieron
      con el panel/endpoint de CU10 (POST .../imagenes/subir, ver
      app/core/media.py) y reemplazaron tanto los placeholders de picsum como
      las URLs externas hotlinkeadas del seed -- ya no depende de terceros.
      Verificado: 40 filas en producto_imagen, todas sirviendo desde
      /media/productos/<id>/..., ninguna con mas o menos de 1 imagen.
      OJO: estas imagenes viven en el volumen Docker `backend_media`, no en
      el repo -- en Railway hace falta el Volume que ya quedo documentado en
      RAILWAY.md paso 5, si no se pierden en el proximo deploy.
      [2026-09-13, mas tarde aun] Bug reportado: al desplazarse entre colores
      en la ficha de producto, el color de catalogo no aparecia primero (el
      selector ordenaba por nombre, no por cual es el color "real" de la foto
      subida) y, si el color coincidia con uno de los stock-photo del seed,
      la foto no correspondia a la subida por el admin. Arreglado:
        - frontend (producto-detalle.page.ts): `colorDeCatalogo` detecta que
          color de variante tiene el mismo imagen_url que la foto principal
          y lo pone primero en `colores()`; ngOnInit ahora selecciona ese
          color por defecto en vez del primero alfabetico.
        - [2026-09-13, sincronizado en movil] el mismo fix se replico en
          `movil/lib/paginas/producto_detalle_pagina.dart`: `VarianteOut`
          (catalogo_models.dart) no traia `imagen_url` por variante, asi que
          era imposible detectar el color de catalogo. Se agrego el campo, un
          `_colorDeCatalogo()`/`_imagenActual()` equivalentes a los computed de
          la web, se reordenan las chips de variante para que el color de
          catalogo aparezca primero, y se selecciona por defecto al cargar la
          pantalla. `flutter analyze` sin issues, `flutter test` 3/3 en verde.
        - datos: para 26 productos donde la foto real subida (es_principal,
          sin color_id) mostraba con claridad un color que YA es variante del
          producto, se le puso ese color_id a la fila y se borro la foto de
          stock redundante que quedaba para ese mismo color (mismo mecanismo
          que pide el negocio: "una imagen por color, la del catalogo gana").
          Hecho a mano contra la base local, NO esta en db/03_datos_iniciales.sql
          -- si se corre `docker compose down -v` se pierde y hay que rehacerlo
          (la lista de que color le toca a cada producto quedo en el historial
          del chat, no en un archivo del repo).
      [2026-09-14] Variantes de color generadas con IA cargadas para 12 de los
      productos de la lista de abajo (recortadas a mano de collages 2x2 en
      backend/3x4/variantes/ y subidas con POST .../imagenes/subir, mismo
      mecanismo que el punto anterior):
        - Completos (foto real por cada color del producto): blazer-entallado
          (Azul/Beige/Negro), falda-lapiz-de-panio (Gris/Vino, Negro ya tenia
          foto), pantalon-de-vestir-sastrero (Azul/Gris, Negro ya tenia foto),
          jean-wide-leg (Azul/Negro, Blanco ya tenia foto).
        - Parciales (mejoraron pero falta 1 color, sigue con foto de stock):
          bota-cania-alta-de-gamuza (Negro/Camel resueltos, falta Gris),
          campera-puffer-impermeable (Negro/Verde Oliva resueltos, falta Rojo),
          falda-midi-plisada (Negro/Vino resueltos, falta Camel),
          falda-larga-de-gasa (Beige resuelto, faltan Blanco y Rosa Palo),
          campera-de-jean-oversize (Negro resuelto, falta Gris),
          cardigan-de-punto-grueso (Gris resuelto, falta Camel),
          pantalon-palazzo-de-lino (Verde Oliva resuelto, falta Blanco),
          legging-termico (Gris resuelto vía "azul pizarra"; Azul se dejo con
          la foto de stock porque no habia un recorte que le quedara mejor).
        - Sin tocar a proposito: borcego-negro-con-cordones y
          vestido-negro-de-fiesta (unico color, ya tenian foto real);
          falda-de-jean-corta (ningun recorte de esa tanda -- lavados de jean
          y un verde oliva -- correspondia a sus colores reales Azul/Blanco/
          Negro); vestido-largo-estampado (unica variante Beige y es un
          estampado, sigue sin corresponder ninguna reinterpretacion de color,
          ver nota de abajo).
        Los 17 collages originales (los 16 recortados + un duplicado) quedaron
        guardados sin borrar en backend/3x4/variantes/_collages_originales/
        por si hace falta revisarlos o recortar el color que falto en cada
        parcial.
      [2026-09-14, mas tarde] Estas 20 imagenes se volvieron OFICIALES y ya
      estan en db/03_datos_iniciales.sql (sobreviven a `docker compose down -v`,
      probado de punta a punta):
        - Los 20 archivos quedaron versionados en
          backend/app/media_semilla/variantes/ (nombrados
          <slug-producto>-<color>.jpg).
        - app/main.py copia esa carpeta a MEDIA_DIR en cada arranque del
          backend (el volumen `backend_media` es efimero, media_semilla no),
          asi que los archivos existen en disco antes de que Postgres inserte
          las filas que apuntan a ellos.
        - Las 20 filas de producto_imagen en la seccion "IMAGENES POR COLOR"
          de db/03_datos_iniciales.sql se actualizaron para apuntar a
          http://localhost:8081/media/variantes/<archivo>.jpg en vez de a los
          istockphoto hotlinkeados (que siguen ahi para los colores que no
          tienen recorte, ver la lista de parciales arriba).
        - OJO Railway: la URL sigue hardcodeada a localhost:8081 porque
          producto_imagen.url tiene que ser absoluta y PUBLIC_BASE_URL cambia
          por entorno (igual que las fotos reales de mas arriba). En Railway
          estos 20 colores no van a cargar hasta subir los mismos archivos de
          media_semilla/variantes/ por el panel de CU10 contra esa base, o
          hasta armar una version del seed parametrizada por entorno.
        - ERROR cometido al probar esto con `docker compose down -v`: se
          penso que solo se perdia el color_id de la fila es_principal de 26
          productos (parrafo de arriba, [2026-09-13 mas tarde]), pero en
          realidad `down -v` borra el volumen Docker `backend_media` entero
          -- ahi vivian los 40 ARCHIVOS de las fotos reales de catalogo
          subidas por CU10 (no solo su color_id). Las 40 fotos principales
          quedaron rotas (volvieron a los hotlinks/picsum del seed viejo)
          hasta que se detecto y se corrigio en la misma sesion, ver
          [2026-09-14, mas tarde aun] debajo. El color_id de esos 26
          productos si se perdio de verdad y sigue sin recuperar (esa lista
          no quedo en ningun archivo del repo).
      [2026-09-14, mas tarde aun] Recuperadas y hechas OFICIALES las 40 fotos
      de catalogo (es_principal) que se rompieron en el punto anterior:
        - Los 40 archivos originales seguian en backend/3x4/<nombre>.jpg (la
          fuente de la que se habian subido por CU10 la primera vez) y se
          copiaron a backend/app/media_semilla/catalogo/<slug>.jpg.
        - db/03_datos_iniciales.sql reemplazo el bloque entero de 40 INSERT
          con URLs hotlinkeadas por un solo INSERT ... SELECT que arma
          http://localhost:8081/media/catalogo/<slug>.jpg para cada producto
          -- mas corto y ya no depende de que ningun link externo siga vivo.
        - Probado con `docker compose down -v && up --build` completo: 40 de
          40 fotos principales sirviendo en 200 desde /media/catalogo/, y las
          20 variantes de color del punto anterior tambien sobreviven juntas.
      [2026-09-14, ya para terminar] color_id de la foto principal para los 40
      productos, hecho de nuevo y esta vez SI en el seed (la version de
      2026-09-13 se habia hecho a mano contra la base y se perdio con el
      down -v de este mismo dia). db/03_datos_iniciales.sql arma esta vez el
      color_id con un JOIN contra una lista fija de (slug, color) en el mismo
      INSERT que carga las 40 fotos, asi que sobrevive a cualquier reseed.
      Se revisaron las 40 fotos una por una contra los colores reales de cada
      producto (mismo criterio del punto anterior: foto vs. hex de la
      variante). Resultado -- igual a la cuenta que ya habia antes de
      perderse, 26 con match y 14 sin (buena señal de que el criterio es
      consistente):
        26 CON color asignado: tapado-de-lana-largo=Camel,
        campera-de-jean-oversize=Azul, cardigan-de-punto-grueso=Beige,
        chalina-de-alpaca=Gris, cartera-de-cuero-bandolera=Camel,
        sombrero-de-ala-ancha=Beige, cinturon-de-cuero-hebilla-dorada=Negro,
        gorro-y-guantes-de-lana=Gris, lentes-de-sol-redondos=Negro,
        blusa-de-lino-manga-corta=Blanco, top-de-tirantes-de-algodon=Negro,
        blusa-cropped-con-volados=Blanco, borcego-negro-con-cordones=Negro,
        falda-de-jean-corta=Azul, falda-lapiz-de-panio=Negro,
        jean-tiro-alto-skinny=Azul, pantalon-palazzo-de-lino=Beige,
        pantalon-de-vestir-sastrero=Negro, jean-wide-leg=Blanco,
        legging-termico=Negro, sandalia-de-plataforma=Blanco,
        sandalia-baja-de-cuero=Camel, vestido-negro-de-fiesta=Negro,
        zapato-stiletto-de-charol=Negro, ballerina-de-cuero=Beige,
        zapatilla-urbana-blanca=Blanco.
        14 SIN color asignado (foto blanca/marfil/estampada/multicolor, o
        color ambiguo entre dos tonos parecidos -- se prefirio dejar sin
        preseleccion antes que forzar un color que no coincide con la foto):
        blazer-entallado, campera-puffer-impermeable,
        blusa-de-seda-manga-larga (marfil: Beige o Blanco, ambiguo),
        camisa-oversize-a-cuadros (a cuadros), botineta-de-cuero-con-taco,
        bota-cania-alta-de-gamuza, falda-midi-plisada, falda-larga-de-gasa,
        sandalia-con-tiras-y-taco, vestido-floral-manga-corta (floral),
        vestido-midi-plisado (marron chocolate, no calza con Camel ni Vino),
        vestido-de-punto-manga-larga (a rayas), vestido-camisero-de-lino
        (marfil: Beige o Blanco, ambiguo), vestido-largo-estampado
        (estampado, unica variante Beige que no corresponde).
      Probado con `docker compose down -v && up --build` completo: los 26
      colores preseleccionan la foto de catalogo correcta (verificado en la
      API para cardigan-de-punto-grueso, jean-wide-leg y
      pantalon-de-vestir-sastrero), los otros 14 y las 20 variantes de color
      del punto anterior siguen sin romperse, 150 filas en producto_imagen en
      total (sin cambios, porque esto solo le puso color_id a filas que ya
      existian).
      QUEDA PENDIENTE (cosmetico, no rompe nada): para esos 26 productos
      quedo una fila redundante en la seccion "IMAGENES POR COLOR" mas abajo
      -- una foto de stock vieja para el mismo color que ahora ya tiene la
      foto real. La query del backend ya prioriza es_principal (ORDER BY
      es_principal DESC antes de tomar la primera fila), asi que no se nota,
      pero convendria borrar esas 26 filas de stock en algun momento para no
      dejar basura en el seed.

      QUEDA PENDIENTE (superado por el punto [2026-09-14, ya para terminar]
      de mas arriba, que ya asigna color_id en el seed para 26 productos y
      deja la lista actualizada de los 14 sin match -- esta entrada vieja
      queda solo como historial de por que sombrero-de-ala-ancha y
      vestido-camisero-de-lino se resolvieron distinto: sombrero-de-ala-ancha
      se decidio como Beige arriba pese a la ambiguedad Beige/Camel;
      vestido-camisero-de-lino sigue sin asignar porque la ambiguedad
      Beige/Blanco no se pudo resolver a simple vista):
        Variantes de color generadas con IA (CATALOGO_DEMO.txt punto 1 y 4.5)
        siguen pendientes para los productos de esta lista que son destacados
        y no son estampados (BOT-001, FAL-001, BLU-002 -- PAN-002, ABR-001,
        SAN-001, ZAP-003 y ACC-002 ya quedaron resueltos arriba).

[2.10] [HECHO 2026-09-18] CU17 "Recibir Recomendaciones de IA": backend + web.
      Aclaracion del usuario que quedo registrada: "IA" aca NO es un modelo (Claude, Gemini, etc)
      activo, es un algoritmo sobre el historial del cliente. Modulo nuevo
      app/modules/recomendaciones/ (GET /recomendaciones, cualquier CLIENTE autenticado, 403 para
      STAFF), sin permiso de CU13 de por medio porque no es pantalla de gestion. Algoritmo, todo
      en SQL crudo en el router, dos pasadas:
        1. "Personalizadas": categorias donde el cliente compro (peso 2) o reservo (peso 1) mas
           (top 3), completadas con productos activos de esas categorias que todavia no toco.
           motivo = "Porque te gusta <categoria>".
        2. "Generales" (relleno si 1 no alcanza el limite pedido, o directamente si el cliente no
           tiene historial): destacados primero, despues los mas vendidos de toda la cadena.
           motivo = "Destacado en FashionStore" / "Tendencia en FashionStore".
      Nunca se recomienda un producto que el cliente ya compro o reservo (se excluye por id en
      las dos pasadas). Probado con curl contra Postgres real: cliente@fashionstore.bo (con una
      compra en Accesorios) recibe solo Accesorios con motivo de historial; cliente2 (sin
      historial) recibe puro fallback de destacados; admin pidiendo el endpoint da 403.
      Reusa el mismo shape de ProductoOut que /catalogo/productos + un campo `motivo`; no se
      importa el schema de catalogo (cada modulo mantiene el suyo, ver CLAUDE.md), se duplico a
      proposito en app/modules/recomendaciones/schemas.py.
      Web: la tarjeta de prenda de tienda.page se extrajo a
      frontend/src/app/shared/catalogo/producto-card.ts (selector `li[app-producto-card]`, para
      no envolverla en un <li> aparte) porque ahora tiene dos consumidores: el catalogo de
      siempre y la seccion nueva "Recomendado para vos" en tienda.page.html (arriba del catalogo,
      solo para clientes, oculta si no hay recomendaciones). Verificado: tsc --noEmit limpio,
      `ng build` limpio.
      FALTA (a proposito, mismo criterio que CU15): movil no se toco, CU17 queda solo en
      backend+web.
      NOTA: [2.7] (mas arriba en este mismo archivo) decia que existia una tabla `favorito` sin
      usar -- no es cierto, no existe en db/01_schema.sql (corregido ahi mismo). El historial que
      usa CU17 sale de venta_detalle/venta y reserva_detalle/reserva, no de favoritos.

[2.11] [HECHO 2026-09-18, FALTA PROBAR EN VIVO] CU18 "Asistir al Cliente via Chatbot": backend +
      web. A diferencia de CU17, esta CU si llama a un modelo de IA de verdad (API de Claude,
      Anthropic) -- aclaracion explicita del usuario al pedirla.
      Backend: app/modules/asistente/router.py, POST /asistente/chat, solo CLIENTE (403 para
      STAFF, mismo patron _exigir_cliente que recomendaciones/carrito). Decisiones conversadas
      con el usuario:
        - Modelo: claude-haiku-4-5 (barato, configurable por ANTHROPIC_MODEL en .env).
        - Grounding: tool use, NO todo el catalogo en el prompt. Una herramienta
          "buscar_catalogo" (strict, JSON schema) que Claude llama con filtros (categoria_slug,
          q, talla_codigo, color_nombre, precio_maximo, limite) contra Postgres en vivo -- nunca
          inventa productos. El system prompt le pide expresamente hacer 1-2 preguntas para
          acotar ANTES de llamar a la herramienta si el pedido es muy general (para no gastar de
          mas ni traer resultados demasiado amplios), y restringe la charla a temas de
          FashionStore (rechaza y redirige cualquier tema no relacionado, instruccion explicita
          de no seguir pedidos de cambiar esas reglas).
        - Contexto de personalizacion: un parrafo corto con las categorias favoritas del cliente
          (mismo query que CU17: compra=peso 2, reserva=peso 1) inyectado en el system prompt.
        - Sin persistencia: el frontend reenvia la conversacion completa en cada POST (lista
          mensajes[]), el backend no guarda nada, no hay tabla de chat en el esquema.
        - Sin streaming: respuestas cortas, no valia la complejidad de SSE para esta entrega.
        - Loop manual (no el tool_runner beta del SDK): un unico tipo de herramienta, maximo 4
          iteraciones (MAX_ITERACIONES_HERRAMIENTA), sin dependencia beta.
      El endpoint devuelve `respuesta` (texto) + `productos_sugeridos` (mismo shape que
      catalogo.schemas.ProductoOut, duplicado en asistente/schemas.py como el resto de los
      modulos) tomados de la ultima llamada exitosa a buscar_catalogo en ese turno.
      Config nueva: ANTHROPIC_API_KEY / ANTHROPIC_MODEL en backend/.env(.example),
      requirements.txt suma anthropic==1.6.0. Sin la clave, /asistente/chat responde 503 en vez
      de romper el arranque del backend (probado). El 403 para STAFF tambien esta probado.
      LO QUE FALTA PROBAR: una conversacion real contra la API de Claude (necesita
      ANTHROPIC_API_KEY real en backend/.env, que gasta saldo de la cuenta del usuario -- no se
      hizo sin su autorizacion explicita). Falta confirmar en vivo: que el loop de la herramienta
      funcione de punta a punta, que el modelo efectivamente pregunte antes de buscar cuando el
      pedido es vago, y que rechace temas fuera de la tienda.
      Web: pages/asistente/ (ruta /asistente, link nuevo en la cabecera de tienda.page junto a
      "Mis compras"), burbujas de chat con Bs formateados, reusa <li app-producto-card> (el mismo
      componente de CU17) para mostrar las prendas sugeridas debajo de la respuesta del
      asistente. Verificado: tsc --noEmit limpio, `ng build` limpio.
      FALTA (a proposito, mismo criterio que CU15/CU17): movil no se toco.

[2.12] [HECHO 2026-09-18] CU19 "Consultar Bitacora de Auditoria": backend + web.
      Solo lectura sobre la tabla `auditoria` que ya existia en el esquema (db/01_schema.sql:639)
      y que CU10-CU13 y el login vienen poblando desde antes via app/core/auditoria.py -- esta CU
      no agrega escritura nueva, solo la pantalla para consultarla. Modulo nuevo
      app/modules/auditoria/ con permiso unico `auditoria.leer`, sembrado SOLO para ADMIN (a
      diferencia de reportes.leer, que tambien tiene ENCARGADO): la bitacora es del sistema
      completo -- usuarios, roles, catalogo, sucursales, proveedores, recepciones -- y la tabla
      auditoria no tiene sucursal_id para acotar por sucursal, asi que no hay forma de darsela a
      un ENCARGADO sin que vea movimientos de otras sucursales. Agregado tambien a
      db/reparaciones/permisos_cu13.sql (paso 2, el INSERT de codigos nuevos) para que Railway lo
      reciba corriendo ese script, igual que el resto de la matriz de permisos.
      Dos endpoints, sin capa de servicio, SQL directo en el router (mismo patron que reportes):
      `GET /auditoria` (filtros usuario_id, entidad, entidad_id, accion, desde/hasta, paginado
      con pagina/tamanio_pagina, devuelve total + items) y `GET /auditoria/entidades` (valores
      distintos de `entidad` ya registrados, para el filtro del panel -- no se hardcodea la lista
      porque `entidad` es VARCHAR libre, no un ENUM). `accion` si se valido con pattern fijo
      (CREAR/ACTUALIZAR/ELIMINAR/LOGIN, el ENUM accion_auditoria del schema) porque un valor
      invalido no puede existir nunca, mismo criterio que el `canal` de reportes.
      OJO asyncpg: no hay codec de jsonb configurado en app/core/db.py, asi que datos_antes/
      datos_despues llegan de la consulta como texto JSON, no como dict -- se parsean a mano con
      json.loads en auditoria/router.py (`_parsear_json`). Es la primera vez que el backend LEE
      una columna jsonb (antes solo se escribia via ::jsonb en auditoria.py), asi que si otro
      modulo necesita leer una columna jsonb en el futuro va a pisar el mismo problema.
      Probado con curl contra Postgres real (docker compose down -v && up --build, necesario
      porque se toco 03_datos_iniciales.sql): login de ADMIN aparece solo con accion LOGIN;
      crear un proveedor de prueba genera una fila entidad=proveedor con datos_despues ya parseado
      como objeto JSON (no string); filtro por entidad funciona; accion=BORRAR (invalida) da 422;
      ENCARGADO (encargada.lapaz@fashionstore.bo) y CLIENTE dan 403.
      Web: pages/panel-auditoria/ (ruta /panel/auditoria, entrada nueva en panel.page.ts y
      panel-shell.ts, ambas detras de auth.tienePermiso('auditoria.leer')). Tabla con filtros
      (entidad, accion, ID de entidad, rango de fechas), paginacion Anterior/Siguiente, y una fila
      expandible por evento que muestra datos_antes/datos_despues con JsonPipe cuando existen (los
      eventos LOGIN no tienen, asi que esos no muestran el boton "Ver detalle"). Verificado:
      `npx tsc --noEmit` limpio, `ng build` limpio (los 3 warnings de presupuesto CSS que aparecen
      son de paginas preexistentes, no de esta).
      FALTA (a proposito, mismo criterio que CU15/CU17/CU18): movil no se toco. No se pudo probar
      la pantalla en un navegador real en esta sesion (sin herramienta de captura/automatizacion
      de navegador disponible) -- si algo se ve mal visualmente, avisar para ajustarlo.

[2.13] [HECHO 2026-09-18] CU20 "Entrega a Domicilio (Delivery)": backend + web + movil.
      Cierra tambien el pendiente 2.4 (la entrega a domicilio estaba aceptada por la API pero era
      inalcanzable desde el cliente).

      BASE DE DATOS (hay que recrear el volumen: docker compose down -v && up --build):
        - ENUM nuevo estado_envio (PENDIENTE/ASIGNADO/EN_RUTA/ENTREGADO/FALLIDO/CANCELADO) y
          valor nuevo REPARTIDOR en cargo_empleado.
        - venta.costo_envio NUMERIC(12,2). Entra en la BASE IMPONIBLE junto con la mercaderia
          (base = subtotal - descuento + costo_envio, IVA 13% sobre eso): el flete se factura
          con el mismo IVA. Ojo si se comparan totales viejos contra nuevos.
        - Tablas envio (una por venta, UNIQUE venta_id) y envio_evento (bitacora append-only).
          El envio CONGELA la direccion en texto + coordenadas porque la clienta puede editarla
          o borrarla despues y la hoja de ruta tiene que seguir diciendo a donde iba el paquete.
        - fn_cotizar_envio(distancia, monto) en 02_logica.sql: la regla comercial vive en
          Postgres y los parametros en la tabla configuracion (delivery_tarifa_base 8,
          delivery_precio_km 3.5, delivery_costo_minimo 10, delivery_radio_km 12,
          delivery_gratis_desde 800). Cambiar la tarifa NO necesita redeploy.
        - tg_envio_historial: mismo patron que tg_reserva_historial (el backend escribe
          envio.actualizado_por_id en el mismo UPDATE y el trigger firma el evento con eso).
        - Seed: las 3 sucursales ahora tienen lat/long reales (sin eso no hay distancia que
          cobrar), 3 direcciones de clientas con pin, rol REPARTIDOR y usuario
          repartidor.scz@fashionstore.bo (demo1234, Equipetrol).
        - Migracion para una base ya desplegada (Railway): db/reparaciones/cu20_delivery.sql,
          idempotente. PROBADO en los dos caminos: sobre una base sin CU20 (crea todo) y dos
          veces seguidas sobre una que ya lo tiene (no duplica nada).

      BACKEND:
        - app/core/ruteo.py: cliente de openrouteservice (HeiGIT) -- matriz de distancias y
          geocoding Pelias acotado a Bolivia. Se eligio ORS sobre Google Distance Matrix porque
          la key del plan gratuito no pide tarjeta de credito. SIN ORS_API_KEY NO SE ROMPE NADA:
          cae a Haversine x1.35 (factor de sinuosidad urbana) y la cotizacion viaja marcada como
          proveedor HAVERSINE, que ademas queda guardado en envio.proveedor_ruteo. Lo que si se
          pierde sin la key es el buscador de direcciones (devuelve lista vacia y la UI dice por
          que); el pin del mapa se puede poner igual a mano.
        - Modulos nuevos: direcciones/ (CRUD de la clienta, sin permisos de CU13, como carrito/)
          y envios/ (servicio.py + router.py del cliente + admin_router.py del panel).
        - El envio nace en el WEBHOOK del pago, no en el checkout: mientras el pago siga
          PENDIENTE no hay nada que repartir. crear_envio_de_venta es idempotente por el UNIQUE
          de envio.venta_id, asi que un webhook reintentado no duplica.
        - Permisos nuevos envios.leer / envios.actualizar (ADMIN, ENCARGADO y el rol REPARTIDOR).
          Dos recortes de alcance encima del permiso: por sucursal (igual que recepciones/) y por
          repartidor (cargo REPARTIDOR -> solo ve y toca lo suyo).
        - UsuarioOut suma `cargo` (login y /auth/yo) para que las UIs distingan al repartidor.
        - requirements.txt suma httpx==0.28.1 (venia transitivo por anthropic, ahora explicito).
        - PROBADO con curl contra Postgres real: cotizacion (dentro y fuera del radio, con y sin
          envio gratis), CRUD de direcciones (incluido el 409 al borrar una usada en una venta y
          el repase automatico de "principal" al borrarla), checkout a domicilio con la tarifa
          sumada (319.80 + 21.44 de envio -> IVA 44.36 -> total 385.60), webhook que crea el
          envio, asignacion, transicion invalida rechazada (409), circuito
          PENDIENTE->ASIGNADO->EN_RUTA->ENTREGADO con la venta pasando a ENTREGADA, y la
          bitacora firmada con el usuario correcto en cada paso.

      WEB: core/direcciones/ + core/envios/, pages/mis-direcciones (libreta con mapa Leaflet,
      buscador de direcciones y "usar mi ubicacion"), selector de entrega + tarifa explicada en
      el carrito, seguimiento con linea de tiempo en pages/compra, y pages/panel-envios
      (/panel/envios, permisoGuard envios.leer/actualizar) con contadores, cola, mapa del destino,
      que lleva el pedido, asignacion y bitacora. Dependencia nueva: leaflet (+ @types/leaflet en
      devDependencies); leaflet.css se carga desde angular.json y se agrego
      allowedCommonJsDependencies para el aviso de CommonJS. Verificado: tsc --noEmit limpio y
      `ng build` limpio (solo quedan los 3 warnings de presupuesto CSS preexistentes).

      MOVIL: core/direcciones/ + core/envios/, paginas/mis_direcciones_pagina.dart (mapa con
      flutter_map, dependencia nueva junto con latlong2), entrega + cotizacion en el carrito,
      seguimiento en compra_pagina y paginas/panel_envios_pagina.dart. Es la CU que mas gana en
      el telefono: el repartidor marca "en camino" y "entregado" desde la calle. Verificado:
      `flutter analyze` sin issues y `flutter test` en verde.

      FALTA / A TENER EN CUENTA:
        - No se probo con ORS_API_KEY real (no habia clave en esta sesion): todo lo verificado
          uso el respaldo Haversine. Con la key hay que confirmar la matriz de distancias y el
          geocoding. La clave se saca gratis en https://openrouteservice.org/dev/#/signup y va
          en backend/.env como ORS_API_KEY.
        - Las pantallas no se pudieron ver en un navegador ni en el emulador en esta sesion
          (mismo limite que CU19): compilan, pero si algo se ve mal visualmente, avisar.
        - No hay reintento automatico ni notificacion push cuando el envio cambia de estado; la
          clienta se entera al abrir la pantalla de la compra (hay una notificacion en la tabla
          `notificacion` cuando se crea el envio, igual que el resto de los avisos del sistema).
        - Los reportes de CU15 no abren todavia el costo_envio como columna propia; el monto ya
          esta incluido en venta.total.

[2.14] [HECHO 2026-09-18] Checkout web: Stripe embebido + Efectivo y QR (CU05/CU06).
      Pedido del usuario: 3 metodos de pago (Stripe, Efectivo, QR) y que Stripe se pague
      inline en el carrito, sin redirigir a checkout.stripe.com, precargando el script para
      que no se sienta lento al elegirlo.

      BASE DE DATOS: el ENUM pasarela_pago pasa de ('STRIPE','LIBELULA') a ('STRIPE','QR')
      -- 'QR' es la misma pasarela simulada de siempre (pantalla /pago-simulado), solo
      cambia el nombre. metodo_pago ya tenia 'EFECTIVO' desde el esquema original (lo usaba
      CU07/POS), asi que Efectivo en el checkout web no agrega columnas ni tablas nuevas.
      Migracion para una base ya desplegada: db/reparaciones/pagos_efectivo_qr.sql
      (ALTER TYPE ... RENAME VALUE, idempotente).

      BACKEND: CheckoutIn.pasarela se reemplaza por metodo_pago: STRIPE | QR | EFECTIVO.
        - STRIPE en canal WEB pide un Checkout Session con ui_mode="embedded" y
          redirect_on_completion="never" (Stripe.js la monta inline con el client_secret
          que ahora devuelve CheckoutOut); en canal MOVIL sigue pidiendo el Checkout
          hospedado de siempre (Flutter no tiene SDK embebido, sigue abriendo el navegador
          del telefono -- ver 4.1).
        - QR es LIBELULA de toda la vida, solo con el nombre nuevo.
        - EFECTIVO no pasa por ninguna pasarela: decision del usuario en la sesion de
          diseño fue que se aprueba al toque en el checkout mismo (retiro lo cobra la
          sucursal, domicilio queda a cargo del servicio de delivery, se asume cobrado sin
          confirmacion posterior) -- mismo resultado que un webhook aprobado, pero
          sincrono. Para que ventas/router.py pudiera aprobar un pago sin pasar por
          pagos/router.py (evitando un ciclo de imports), la logica de
          confirmar_aprobado/confirmar_rechazado que antes vivia en pagos/router.py se
          extrajo a app/modules/pagos/servicio.py (mismo patron que envios/servicio.py: la
          excepcion documentada a "cada modulo se basta a si mismo"), junto con
          _alertar_stock_bajo (antes en ventas/router.py). pagos/router.py, ventas/router.py
          y envios/servicio.py orbitan alrededor de este archivo sin ciclos.
        - GET /pagos/config expone stripe_publishable_key (no es secreta) para que el
          frontend no la tenga que hornear en el build -- cambia por entorno sin recompilar.
      Probado: `python -c "import app.main"` limpio, OpenAPI genera /pagos/config y
      /ventas/checkout con el schema nuevo.

      WEB: dependencia nueva @stripe/stripe-js. carrito.page.ts precarga loadStripe() (con
      la key de /pagos/config) en ngOnInit, cacheada en una promesa que vive mientras el
      componente esta montado. A diferencia de la primera version de este punto, elegir un
      metodo de pago YA CARGA lo que corresponde (no hace falta tocar "Pagar" para verlo):
        - STRIPE (pasarela por defecto): se arma el checkout apenas se conoce el carrito (o al
          tocar el boton "Stripe") y `stripe.createEmbeddedCheckoutPage({clientSecret})` se monta
          en un <div #stripeContenedor>, adentro del bloque @switch(metodoPago) === 'STRIPE'. El
          boton de pago es el del propio formulario de Stripe -- no hay uno aparte afuera.
        - QR: muestra `public/pago-qr.jpeg` (copiada del archivo que paso el usuario) mas un
          boton "Pagar con QR" debajo, que recien ahi llama al checkout (no hace falta pegarle a
          la API solo para mostrar la imagen, es estatica).
        - EFECTIVO: el texto de siempre mas su propio boton "Pagar con efectivo" debajo.
        Cambiar de sucursal, de direccion o de "como lo queres recibir" con Stripe ya montado
        (`actualizarCotizacionYPago()` / `elegirEntrega()`) vuelve a armar el Checkout Session
        para que el monto mostrado sea el correcto -- si no, quedaba mostrando el total viejo.
      Verificado: `npx tsc --noEmit` limpio, `ng build` limpio (mismos warnings de presupuesto
      CSS preexistentes de otras paginas + ~350 bytes nuevos en carrito.page.css), la imagen
      sirve en 200 desde `/pago-qr.jpeg` (carpeta `public/`, Angular 20).

      MOVIL: mismo contrato de API (metodo_pago en vez de pasarela, QR en vez de LIBELULA),
      boton "Efectivo" nuevo en carrito_pagina.dart. Sin el rediseño de la web (el usuario pidio
      esto "en web" explicitamente): sigue siendo elegir metodo -> tocar "Pagar" -> recien ahi
      se arma el checkout, para los tres metodos. Stripe sigue abriendo el navegador del telefono
      (ver 4.1), no hay SDK embebido en Flutter en esta entrega. Verificado: `flutter analyze`
      sin issues, `flutter test` 3/3.

      BUG ENCONTRADO Y CORREGIDO (reportado por el usuario probando en vivo): "en Stripe no me
      aparece el monto que deberia pagar y en QR/Efectivo me carga el Stripe". Causa real: el
      "reintento de checkout" (`POST /ventas/checkout` con una venta+pago ya PENDIENTE para ese
      carrito) devolvia esa venta pendiente sin importar que pasarela pedia el body -- si el
      cliente empezaba con Stripe, no completaba el pago, y despues elegia QR o Efectivo, el
      backend le devolvia la MISMA sesion de Stripe vieja (a veces con el monto de un carrito
      anterior). Arreglado en `iniciar_checkout` (ventas/router.py): el reintento solo se reusa
      si `pendiente["pasarela"] == body.metodo_pago`; si el metodo cambio, el intento anterior se
      marca ANULADA/RECHAZADO (y se revierte el uso del cupon si tenia uno) y se cae al resto de
      la funcion, que arma una venta/pago nuevos para el metodo recien elegido. Probado con curl
      contra Postgres y Stripe reales, cadena completa Stripe -> QR -> Efectivo sobre el mismo
      carrito: cada cambio de metodo crea una venta nueva con el monto correcto (508.16 en la
      prueba) y dos ventas quedan ANULADA/RECHAZADO sin romper nada; Efectivo aprueba al toque
      (PAGADA, venta_detalle, comprobante, stock descontado); QR queda PENDIENTE y el webhook
      simulado la aprueba (`/pagos/webhook/QR` 200, `/pagos/webhook/LIBELULA` ahora 404); Stripe
      en canal WEB devuelve `client_secret` real (`cs_test_...`) con el total correcto y
      `url_pago: null`; el mismo metodo en canal MOVIL devuelve `url_pago` hospedada.

      FALTA: probar el Checkout embebido y la imagen del QR mirando la pantalla en un navegador
      real (esta sesion no tiene herramienta de automatizacion de navegador; se verifico contra
      un `ng serve` corriendo en :3000 que sirve la imagen y contra la API real con curl, pero no
      se vio el render final).
      LIMITACION CONOCIDA: si un checkout STRIPE se inicia por WEB (embebido, sin url) y se
      reintenta el mismo carrito por MOVIL antes de pagar, `_pago_pendiente_stripe` no tiene
      `sesion.url` para devolver y cae al mismo respaldo generico que un error de Stripe
      (`/pago-simulado/{id}`), que para STRIPE es una pantalla rota (el webhook simulado solo
      acepta QR). Mismo patron de fragilidad que ya tenia el codigo original para cualquier
      error de `Session.retrieve`; no se soluciono por ser un cruce de canal muy improbable
      (mismo carrito, mismo checkout pendiente, cambiando de dispositivo antes de pagar).

[2.15] [HECHO 2026-09-18] Dos bugs en "reserva -> compra web" (CU04/CU05), encontrados al
      auditar la logistica reserva/venta/pago/inventario, no relacionados con ningun cambio
      reciente:
        - `POST /carrito/desde-reserva/{reserva_id}` (carrito/router.py) validaba el estado de
          la reserva (permite CONFIRMADA/PREPARADA/CLIENTE_PRESENTE) pero despues filtraba los
          items con `estado_item IN ('RESERVADO','PROBADO')`: si el Encargado ya habia preparado
          la reserva (CU08 paso 2), esos items quedan en 'PREPARADO' y el endpoint devolvia 409
          "no tiene prendas pendientes de compra" pese a que la reserva seguia siendo valida.
          Arreglado agregando 'PREPARADO' al filtro.
        - `POST /ventas/checkout` (ventas/router.py::iniciar_checkout), cuando el carrito viene
          de una reserva, validaba stock contra `inventario.disponible`
          (cantidad_fisica - cantidad_reservada), que por diseño YA excluye lo comprometido por
          la propia reserva del cliente. Si esa era la ultima unidad libre de la sucursal, el
          checkout rechazaba con 409 "sin stock suficiente" una prenda que estaba comprometida a
          nombre del cliente. Arreglado: cuando `carrito.reserva_id` no es null, el chequeo ahora
          valida contra `reserva_detalle` (estado_item RESERVADO/PREPARADO/PROBADO) en vez de
          `inventario.disponible`.
      `estado_item = 'PROBADO'` y `reserva.estado = 'PENDIENTE'` siguen sin escribirse en ningun
      lado del backend (estados muertos del ENUM) -- no se toco, es inofensivo y no bloquea nada.
      Verificado: `python -c "import app.main"` limpio despues del cambio. FALTA probar el flujo
      completo con curl/UI contra Postgres real (reservar -> Encargado prepara -> cliente
      convierte a carrito web -> paga) -- no se hizo en esta sesion.

-------------------------------------------------------------------------------
 3. DEUDA TECNICA
-------------------------------------------------------------------------------

[3.1] CU07 y CU08 siguen autorizando por CARGO, no por permiso.
      get_cajero_actual y get_encargado_actual (app/core/deps.py) validan
      empleado.cargo. Los permisos caja.operar, ventas.pos y reservas.atender
      existen y estan asignados, pero no se usan. Al tocar esos modulos hay que
      migrarlos a requiere_permiso(), como ya indica CLAUDE.md.

[3.2] [HECHO 2026-09-14] db/aplicar_en_railway.sh rompia el primer arranque local.
      docker-compose monta ./db entero en docker-entrypoint-initdb.d, y Postgres
      ejecutaba ahi tanto los .sql como los .sh: el script de Railway se corria
      solo en el init y fallaba. Arreglado moviendolo a scripts/aplicar_en_railway.sh
      (RAILWAY.md actualizado con la ruta nueva); DIR dentro del script ahora
      apunta a ../db para seguir encontrando los .sql. Probado con
      `docker compose down -v && up --build`: el contenedor db ya llega a
      "Healthy" y el backend arranca sin esperar.

[3.3] El backend no tiene tests ni linter configurados.
      No existe backend/tests. El movil si tiene (flutter test, 3 casos) y el
      frontend tiene Karma/Jasmine.

[3.4] STRIPE_WEBHOOK_SECRET depende del entorno y no esta documentado en el .env.
      En local sale de `stripe listen --forward-to localhost:8081/pagos/webhook/stripe`;
      en Railway es el signing secret del endpoint creado en el dashboard de
      Stripe. Son distintos y sin el correcto el webhook rechaza la firma.

-------------------------------------------------------------------------------
 4. ESPECIFICO DE LA APP MOVIL
-------------------------------------------------------------------------------

[4.1] Sin deep link de retorno desde Stripe (CU06).
      FRONTEND_URL apunta al frontend web, asi que la success_url de Stripe deja
      al usuario en la WEB, no de vuelta en la app. El pago se confirma igual
      (el webhook es server-to-server) y la app tiene el boton "consultar estado
      del pago", pero el regreso automatico necesitaria applinks o un esquema
      propio. Con QR no pasa: se resuelve dentro de la app. Esto solo aplica al
      movil -- en la WEB (ver 2.14) Stripe ya no redirige a ningun lado.

[4.2] iOS no esta compilado ni probado.
      El proyecto se genero con las plataformas android e ios y el Info.plist ya
      tiene ATS y LSApplicationQueriesSchemes, pero desde Windows no se puede
      compilar. Solo Android esta verificado (emulador Pixel_10a, APK debug y
      release).

[4.3] ndkVersion clavado a una version local.
      android/app/build.gradle.kts fija 30.0.16138531 porque el NDK que pide
      url_launcher_android (28.2.13676358) no esta instalado en esta maquina. En
      otra maquina hay que poner el NDK que tenga instalado, o instalar el 28.2.

[4.4] La URL de produccion esta horneada en el codigo.
      Config.apiUrlProduccion tiene el dominio de Railway. Si se regenera el
      dominio, hay que editarlo y recompilar el APK (no es configurable en
      runtime). Es el mismo compromiso que el API_URL del frontend.

[4.5] El APK de release no esta firmado con una clave propia.
      Usa la debug key (viene asi de `flutter create`). Sirve para instalar por
      USB, pero no para publicar en Play Store.

===============================================================================
 LO QUE SI ESTA HECHO Y VERIFICADO (para no volver a revisarlo)
===============================================================================

- CU01 a CU14 implementados en backend, web y movil. CU14 (Consultar Historial
  de Reservas y Compras) reusa GET /reservas y GET /ventas, que ya devolvian
  todo el historial del cliente; la mitad "reservas" ya estaba en
  mis-reservas.page.ts (web) y mis_reservas_pagina.dart (movil) desde CU04. La
  mitad "compras" ya existia en movil (mis_compras_pagina.dart) pero faltaba en
  la web: se agrego frontend/src/app/pages/mis-compras, con ruta /mis-compras y
  enlace cruzado con /mis-reservas; el detalle reusa /compra/:ventaId, que ya
  existia para el seguimiento post-checkout.
- Movil verificado contra el Docker local y contra Railway, en el emulador
  Pixel_10a, con APK debug y release: login, catalogo, detalle, cuenta de
  cliente vs staff, panel de gestion y CU13 con datos reales.
- Contratos de la API: los 25 endpoints que consume el movil devuelven
  exactamente los campos que esperan los modelos Dart (verificado uno por uno).
- Flujo de pago completo probado de punta a punta contra el backend local:
  checkout (canal MOVIL) -> webhook LIBELULA -> venta PAGADA con comprobante.
- flutter analyze sin issues; flutter test 3/3 en verde.
