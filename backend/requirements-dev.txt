# Dependencias solo para desarrollo/tests, separadas de requirements.txt (produccion) para no
# instalar pytest/ruff en la imagen de Docker. Instalar con:
#   pip install -r requirements-dev.txt
# (ya incluye requirements.txt, no hace falta instalar ese aparte)
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.2
ruff==0.8.6
